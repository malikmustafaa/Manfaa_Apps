import 'package:flutter/material.dart';

class TabDashboardPage extends StatelessWidget {
  const TabDashboardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text(
        'Dashboard',
      )),
    );
  }
}
